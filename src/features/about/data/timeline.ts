/**
 * Company names for the About page's journey timeline — proper nouns, so they
 * stay out of `messages/*.json` (docs/code.md §7). Years, roles and
 * descriptions are translatable copy and live in `messages.about.page.timeline`,
 * in the same order as this list, mirroring how `features/work/data/projects.ts`
 * keeps structural facts separate from copy.
 */
export const TIMELINE_COMPANIES: readonly string[] = [
  "Qweelvo",
  "Annotex",
  "Swapop",
  "iSchool",
];
